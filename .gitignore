# Previously we ignored generated PNGs and the outputs folder.
# PNG plot files are now kept in version control on request, so those
# ignore patterns were removed.

# Ignore any other commonly-generated artifacts
# (keep PDFs tracked; removed `*.pdf` to allow report PDFs to be committed)
