# Minimal pinned dependencies for the analysis scripts
# SciPy installed in the environment; pin to verified version
scipy==1.15.3

# Core data stack
pandas>=1.5.0
numpy>=1.24.0

# Plotting
matplotlib>=3.6.0
seaborn>=0.12.0

# Optional: for statistical tests / report generation
# (SciPy already included above)
# If you use PDF/LaTeX report tools, add them as needed
